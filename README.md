# website-vue
Реализация сайта — контроллера игры на фреймворке Vue.js.

## Сборка
Vite является сервером для разработки. Для продакшна требуется собрать сайт командой `npm run build` и разместить директорию `static` и содержимое директории `dist` на любом удобном веб-сервере.